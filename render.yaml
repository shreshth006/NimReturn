services:
  - type: web
    name: nimreturn-staging-cycle2
    runtime: docker
    plan: free
    region: singapore
    branch: main
    autoDeployTrigger: checksPass
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: HOST
        value: 0.0.0.0
      - key: NIMIQ_NETWORK
        value: TestAlbatross
      - key: NIMIQ_RPC_URL
        sync: false
      # Public community mainnet node. Trusted infrastructure, not proof: see D-039.
      - key: NIMIQ_RPC_URL_MAINALBATROSS
        value: https://rpc.nimiqwatch.com
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: STATIC_ROOT
        value: /app/dist
